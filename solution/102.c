#include <math.h>
#include <stdio.h>

int main() {
  int m[101];

  // -- BEGIN CODE GEN --

  m[0] = 2 + m[2];
  m[43] = 5 - m[((int) pow(4, 4)) * 2 - 2] - 6;
  m[0] = 5 / 2 * 3;
  
  // -- END CODE GEN --

  return 0;
}
