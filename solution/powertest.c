#include <math.h>
#include <stdio.h>

int main() {
  int m[101];

  // -- BEGIN CODE GEN --

  m[0] = 2 + 3;
  m[0] = ((int) pow(2, ((int) pow(2, ((int) pow(2, 2))))));
  printf("%d\n", m[0]);
  
  // -- END CODE GEN --

  return 0;
}
